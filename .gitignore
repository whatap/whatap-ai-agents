.idea/
.venv/
tests/
__pycache__/
*.egg-info/
config.json

# 와탭 에이전트가 WHATAP_HOME 없으면 cwd 에 만드는 것들
whatap.conf
whatap_home
whatap_python.pid*
security.conf
llm_meter.dat
# 루트에 고정한다. 앵커가 없으면 «어느 깊이의» logs/ 든 무시돼서, 팀 디렉토리를
# teams/logs/ 로 만들면 에이전트가 통째로 사라진다 (실제로 겪었다 - git status 는
# 깨끗하고 런타임은 그런 에이전트가 없다고 한다).
/logs/

# 와탭 LLM 에이전트 홈. 액세스 키와 네이티브 바이너리가 들어 있어 커밋하면 안 된다.
/whatap/
*.tar.gz
